# Project Rules for Computational Études

## Typst Conventions

### Mathematical Notation
- Use `gt.eq.slant` instead of `>=` for "greater than or equal to"
- Use `lt.eq.slant` instead of `<=` for "less than or equal to"
- These produce the slanted variants that are typographically preferred in mathematical writing
